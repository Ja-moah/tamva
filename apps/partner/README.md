# Partner

Owns institutions, memberships, partner applications, environments, and API-integration metadata. All tenant-scoped resources must be authorized server-side against an institution context; UI filtering is never a security boundary.

